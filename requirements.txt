fastapi==0.95.2
uvicorn[standard]==0.22.0
aiohttp==3.9.5
pydantic==1.10.12
python-multipart==0.0.9
python-dotenv==1.1.1
PyYAML==6.0.2

# OpenAI SDK used in app.py (works with Grok via base_url)
openai==1.51.2

# (Optional) You can keep or remove this; app.py doesn’t need it if you use OpenAI SDK
groq==0.31.0
